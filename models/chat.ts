export type Chat = {
    id: string;
    senderId: string;
    senderName: string;
    message: string;
    roomId: string;
    type: string;
    timestamp: string;
    createdAt: string;
    updatedAt: string;
};
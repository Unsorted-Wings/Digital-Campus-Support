export type Chat = {
    id: string;
    senderId: string;
    receiverId: string;
    message: string;
    roomId: string;
    type: string;
    timestamp: string;
    createdAt: string;
    updatedAt: string;
};